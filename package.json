{
  "name": "marvel-monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "subgraph:publish": "env-cmd lerna run subgraph:publish --stream --parallel"
  },
  "devDependencies": {
    "@apollo/rover": "^0.9.0",
    "lerna": "^5.5.4",
    "env-cmd": "^10.1.0"
  }
}
